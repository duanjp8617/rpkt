%%
#![allow(missing_docs)]
#![allow(unused_parens)]

use crate::{Buf, PktBuf, PktBufMut};
use crate::{Cursor, CursorMut};
%%

packet Mpls {
    header = [
        label = Field {bit = 20},
        experimental_bits = Field {bit = 3, default=@0},
        bottom_of_stack = Field {bit = 1, arg=bool, default=false},
        ttl = Field {bit = 8}
    ]
}