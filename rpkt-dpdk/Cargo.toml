[package]
name = "rpkt-dpdk"
keywords = ["dpdk", "user-space packet processing"]
categories = ["network-programming"]

workspace = ".."
repository.workspace = true
authors.workspace = true
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
libc = "0.2"
arrayvec = "0.7.2"
once_cell = "1.9.0"
run-dpdk-sys = { path = "../rpkt-dpdk-sys", package = "rpkt-dpdk-sys"}
run-packet = {path = "../rpkt", package = "rpkt", optional = true}

[features]
# `multiseg` feature enables non-contiguous `Mbuf` and `Pbuf`
# default = ["multiseg"]
multiseg = ["dep:run-packet"]

[dev-dependencies]
run-time = {path = "../rpkt-time", package = "rpkt-time"}
run-packet = {path = "../rpkt", package = "rpkt"}
ctrlc = { version = "3.0", features = ["termination"]}
smoltcp = "0.8"

[[example]]
name = "jumboframe_tx"
required-features = ["multiseg"]

[[example]]
name = "jumboframe_rx"
required-features = ["multiseg"]